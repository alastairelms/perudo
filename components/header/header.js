export default function Header( children, ...restProps ) {
    return (
        <div className={styles.header-container}>
            <h1 className={styles.header-title}>Perudo</h1>
        </div>
    )
}